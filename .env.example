# Environment variables for Review Central
#
# HOW TO USE:
# 1. For local development or Firebase Studio, create a copy of this file named ".env" or ".env.local".
# 2. Fill in the values in your new ".env" or ".env.local" file. This file will be ignored by git.
# 3. For production deployment on Firebase Hosting, set these variables in the "Environment variables" section of your App Hosting backend settings in the Firebase Console. DO NOT commit secrets to your repository.
#
# Note: Variables prefixed with NEXT_PUBLIC_ are exposed to the browser. Do not put secrets here.

# --- NextAuth.js (Authentication) ---
# This must be a secret, randomly-generated string.
# You can generate one with: openssl rand -hex 32
AUTH_SECRET=

# The canonical URL of your application.
# For local development: http://localhost:9002
# For Firebase Studio: Use the preview URL provided by the Studio.
# For production: Your public domain, e.g., https://your-app.com
NEXTAUTH_URL=

# --- Azure AD Provider (for NextAuth.js) ---
# Get these from your App Registration in the Azure portal.
# See: https://learn.microsoft.com/en-us/entra/identity-platform/quickstart-register-app
AUTH_AZURE_AD_CLIENT_ID=
AUTH_AZURE_AD_CLIENT_SECRET=
AUTH_AZURE_AD_TENANT_ID=

# --- Firebase Client SDK (Public) ---
# These are safe to expose on the client-side.
# Get these from your Firebase project settings -> General tab -> Your apps -> SDK setup and configuration.
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# --- Firebase Admin SDK (Server-Side) ---
# This is a SECRET service account key.
# In your Firebase project settings -> Service accounts -> Generate new private key.
# This generates a JSON file. Base64-encode the *entire contents* of that JSON file and paste the resulting string here.
# For local/Studio, use your ".env" file.
# For production, set this as a secret in your App Hosting environment settings.
FIREBASE_SERVICE_ACCOUNT_KEY_BASE64=
